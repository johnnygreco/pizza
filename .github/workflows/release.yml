name: Release

on:
  push:
    tags: ['v*']

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Validate tag matches package.json
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG_VERSION="$(node -p "require('./package.json').version")"
          if [ "$TAG" != "$PKG_VERSION" ]; then
            echo "::error::Tag v$TAG does not match package.json version $PKG_VERSION"
            exit 1
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run typecheck

      - run: npm test

      - name: Build release artifact
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          STAGING="pizza-${VERSION}"
          mkdir "$STAGING"
          cp -R extensions skills prompts agents "$STAGING/"
          cp LICENSE "$STAGING/"
          tar -czf "pizza-${VERSION}.tar.gz" "$STAGING"

      - name: Create GitHub Release
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#v}"
          PRERELEASE_FLAG=""
          if [[ "$TAG" == *-* ]]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          gh release create "$TAG" "pizza-${VERSION}.tar.gz" --generate-notes $PRERELEASE_FLAG
        env:
          GH_TOKEN: ${{ github.token }}
